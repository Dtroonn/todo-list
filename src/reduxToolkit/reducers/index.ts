import categories from './categories';
import initialize from './initialize';
import todos from './todos';
import formListItemPopup from './formListItemPopup';

const rootReducer = {
    categories,
    initialize,
    todos,
    formListItemPopup,
};

export default rootReducer;
